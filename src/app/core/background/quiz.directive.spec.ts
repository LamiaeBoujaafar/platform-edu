import { QuizDirective } from './quiz.directive';

describe('QuizBackgroundDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new QuizDirective();
    expect(directive).toBeTruthy();
  });
});
