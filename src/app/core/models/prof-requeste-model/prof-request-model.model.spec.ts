import { ProfRequestModel } from './prof-request-model.model';

describe('ProfRequestModel', () => {
  it('should create an instance', () => {
    expect(new ProfRequestModel()).toBeTruthy();
  });
});
