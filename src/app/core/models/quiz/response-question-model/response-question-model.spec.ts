import { ResponseQuestionModel } from './response-question-model';

describe('ResponseQuestionModel', () => {
  it('should create an instance', () => {
    expect(new ResponseQuestionModel()).toBeTruthy();
  });
});
